import numpy as np
import math

class Shape_Formation:

    #initialize the starting position, the angle of the robot with the reference axis,and the final position
    def __init__(self,current_x,current_y,theta_robot,final_x,final_y):
        self.current_x = current_x
        self.current_y = current_y
        self.theta_robot = theta_robot
        self.final_x = final_x
        self.final_y = final_y

    def
